import { ReactElement } from "react";
import CardCreateForm from "../../components/cardCreateForm/CardCreateForm";

const CreateProductPage = (): ReactElement => {
  return <CardCreateForm />;
};

export default CreateProductPage;
